spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/pothole_reporting}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate.ddl-auto: update
    open-in-view: true
  servlet.multipart:
    max-file-size: 100MB
    max-request-size: 100MB
aws:
  region: ${AWS_REGION:ap-southeast-1}
  bucket: ${AWS_S3_BUCKET:}
  public-base-url: ${AWS_S3_PUBLIC_BASE_URL:}
detection:
  model-url: ${POTHOLE_MODEL_URL:}
  token: ${POTHOLE_MODEL_TOKEN:}
  minimum-confidence: ${POTHOLE_MINIMUM_CONFIDENCE:0.40}
civic:
  webhook: ${CIVIC_REPORTING_WEBHOOK:}
app:
  # Comma-separated browser origins. Override with APP_CORS_ORIGINS when deployed.
  cors-origins: ${APP_CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173}
